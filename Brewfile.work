# Brewfile.work — packages only for work machines
# Installed automatically by chezmoi run_once script when machine_type = work

brew "colima"               # Docker runtime (company policy: no Docker Desktop)
brew "docker"               # Docker CLI (colima provides the runtime)
brew "docker-compose"       # compose plugin for colima
